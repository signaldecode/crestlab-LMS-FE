/**
 * 주문 내역 페이지
 * - 사용자의 강의 구매/주문 내역을 목록으로 보여준다
 * - GET /api/me/orders 엔드포인트에서 데이터를 가져온다
 */

export default function OrdersPage() {
  return (
    <section className="orders-page">
      {/* 주문 내역 리스트가 렌더링된다 */}
    </section>
  );
}
