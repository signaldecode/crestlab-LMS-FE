/**
 * 강의 페이지 상단 배너 (CourseBanner)
 * - 프로모션/이벤트 배너 영역
 * - 실제 이미지/텍스트는 추후 data 연동 시 삽입한다
 */

import type { JSX } from 'react';

export default function CourseBanner(): JSX.Element {
  return (
    <section className="course-banner" aria-label="강의 배너">
      {/* 배너 이미지/슬라이드가 렌더링될 영역 */}
    </section>
  );
}
