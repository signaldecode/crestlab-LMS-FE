/**
 * 강의 정렬 드롭다운 (CourseSort)
 * - 강의 목록 상단 우측에 위치하는 정렬 기준 선택
 * - data.pages.courses.list.sortOptions 기반
 */

import type { JSX } from 'react';

export default function CourseSort(): JSX.Element {
  return (
    <div className="course-sort">
      {/* 정렬 Select 드롭다운이 렌더링될 영역 */}
    </div>
  );
}
