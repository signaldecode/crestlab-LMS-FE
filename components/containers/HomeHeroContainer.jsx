/**
 * 홈 히어로 컨테이너 (HomeHeroContainer)
 * - 홈 페이지 최상단의 메인 배너/히어로 섹션을 조립한다
 * - 배너 이미지, 메인 타이틀, CTA 버튼 등을 data에서 가져와 렌더링한다
 */

export default function HomeHeroContainer() {
  return (
    <section className="home-hero-container">
      {/* data 기반 히어로 배너/타이틀/CTA가 렌더링된다 */}
    </section>
  );
}
