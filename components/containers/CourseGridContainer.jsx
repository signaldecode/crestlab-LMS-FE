/**
 * 강의 그리드 컨테이너 (CourseGridContainer)
 * - 강의 목록 페이지에서 전체 강의를 카드 그리드로 보여주는 섹션
 * - 필터(카테고리/레벨), 정렬, 검색, 페이지네이션을 조합한다
 */

export default function CourseGridContainer() {
  return (
    <section className="course-grid-container">
      {/* 필터 + 강의 카드 그리드 + 페이지네이션이 렌더링된다 */}
    </section>
  );
}
